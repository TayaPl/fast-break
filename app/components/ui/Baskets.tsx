import { StyleSheet } from "react-native";
import React, { FC } from "react";

interface BasketsProps {}

const Baskets: FC<BasketsProps> = ({}) => {
  return <></>;
};

const styles = StyleSheet.create({});

export default Baskets;
